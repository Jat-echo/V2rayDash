# xray 同步按钮 & 批量分配串行化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增手动同步 xray 配置的按钮，并将批量分配改为串行 + 最后统一同步，消除并发竞争导致部分账号未同步的问题。

**Architecture:** 后端新增 `POST /servers/:id/sync-xray` 端点（同步调用 `SyncAllToRemote`，明确返回成功/失败）；前端操作栏新增 `SyncOutlined` 图标按钮；`handleAssign` 从 `Promise.allSettled` 并发改为 `for-of` 串行，结束后自动调用一次 syncXray。

**Tech Stack:** Go (Gin), React 18 + TypeScript, Ant Design 5

---

## 文件变动一览

| 操作 | 文件 | 改动 |
|---|---|---|
| Modify | `backend/internal/handler/server.go` | 新增 `accountSvc` 字段、更新构造函数、新增 `SyncXray` handler |
| Modify | `backend/internal/handler/routes.go` | 注册 `POST /servers/:id/sync-xray` |
| Modify | `frontend/src/services/api.ts` | 新增 `serverAPI.syncXray` |
| Modify | `frontend/src/pages/servers/index.tsx` | 新增图标、状态、handleSyncXray；修改 handleAssign |

---

## Task 1: 后端 SyncXray handler

**Files:**
- Modify: `backend/internal/handler/server.go:1-25`
- Modify: `backend/internal/handler/routes.go:82`

- [ ] **Step 1: 扩展 ServerHandler struct 和构造函数**

找到 `server.go` 第 1-25 行，将 import 区块和 struct+构造函数替换为：

```go
package handler

import (
	"database/sql"
	"fmt"
	"io"
	"net/http"

	"github.com/gin-gonic/gin"
	"v2ray-dash/backend/internal/model"
	"v2ray-dash/backend/internal/repository"
	"v2ray-dash/backend/internal/service"
	"v2ray-dash/backend/internal/ssh"
)

type ServerHandler struct {
	repo       *repository.ServerRepository
	logRepo    *repository.LogRepository
	accountSvc *service.AccountService
}

func NewServerHandler(db *sql.DB) *ServerHandler {
	accountRepo := repository.NewAccountRepository(db)
	serverRepo := repository.NewServerRepository(db)
	return &ServerHandler{
		repo:       serverRepo,
		logRepo:    repository.NewLogRepository(db),
		accountSvc: service.NewAccountService(accountRepo, serverRepo),
	}
}
```

- [ ] **Step 2: 在 RestartXray 函数之后添加 SyncXray handler**

找到 `RestartXray` 函数末尾（约第 121 行的 `}`），在其后插入：

```go
func (h *ServerHandler) SyncXray(c *gin.Context) {
	id := c.Param("id")
	server, err := h.repo.GetByIDForInstall(id)
	if err != nil {
		c.JSON(http.StatusNotFound, gin.H{"error": "server not found"})
		return
	}

	var auth ssh.SSHAuth
	if server.SSHKeyType == "password" {
		auth = &ssh.PasswordAuth{Password: server.SSHPassword}
	} else {
		auth = &ssh.KeyAuth{PrivateKey: server.SSHKey}
	}

	if err := h.accountSvc.SyncAllToRemote(id, auth); err != nil {
		c.JSON(http.StatusInternalServerError, gin.H{"error": fmt.Sprintf("同步失败: %v", err)})
		return
	}

	c.JSON(http.StatusOK, gin.H{"message": "同步成功"})
}
```

- [ ] **Step 3: 在 routes.go 注册新路由**

找到第 82 行：
```go
		api.POST("/servers/:id/restart-xray", serverHandler.RestartXray)
```

在其后插入：
```go
		api.POST("/servers/:id/sync-xray", serverHandler.SyncXray)
```

- [ ] **Step 4: 编译验证**

```bash
cd /home/jat-id/Project/V2rayDash/backend
/usr/local/go/bin/go build ./...
```

预期：无错误输出。

- [ ] **Step 5: Commit**

```bash
cd /home/jat-id/Project/V2rayDash
git add backend/internal/handler/server.go backend/internal/handler/routes.go
git commit -m "feat(backend): add POST /servers/:id/sync-xray endpoint"
```

---

## Task 2: 前端同步按钮

**Files:**
- Modify: `frontend/src/services/api.ts:174`
- Modify: `frontend/src/pages/servers/index.tsx`（多处）

- [ ] **Step 1: api.ts 新增 syncXray 方法**

找到 `serverAPI` 对象（约第 168 行），在 `restartXray` 那行之后添加：

```ts
  syncXray: (id: string) => api.post(`/servers/${id}/sync-xray`).then(r => r.data),
```

最终 `serverAPI` 应为：
```ts
export const serverAPI = {
  list: () => api.get<Server[]>('/servers').then(r => r.data),
  get: (id: string) => api.get<Server>(`/servers/${id}`).then(r => r.data),
  create: (data: Partial<Server>) => api.post<Server>('/servers', data).then(r => r.data),
  update: (id: string, data: Partial<Server>) => api.put(`/servers/${id}`, data),
  delete: (id: string) => api.delete(`/servers/${id}`),
  restartXray: (id: string) => api.post(`/servers/${id}/restart-xray`).then(r => r.data),
  syncXray: (id: string) => api.post(`/servers/${id}/sync-xray`).then(r => r.data),
}
```

- [ ] **Step 2: 新增 SyncOutlined 到 icons import（servers/index.tsx 第 3 行）**

找到第 3 行：
```ts
import { CloudUploadOutlined, EditOutlined, TeamOutlined, ReloadOutlined, DeleteOutlined } from '@ant-design/icons'
```

替换为：
```ts
import { CloudUploadOutlined, EditOutlined, TeamOutlined, ReloadOutlined, DeleteOutlined, SyncOutlined } from '@ant-design/icons'
```

- [ ] **Step 3: 新增 syncingXray 状态变量**

找到约第 93 行：
```ts
  const [restartingXray, setRestartingXray] = useState<string | null>(null)
```

在其后插入：
```ts
  const [syncingXray, setSyncingXray] = useState<string | null>(null)
```

- [ ] **Step 4: 新增 handleSyncXray 函数**

找到 `handleRestartXray` 函数（约第 168 行），在其后插入：

```ts
  const handleSyncXray = async (serverId: string, e: React.MouseEvent) => {
    e.stopPropagation()
    setSyncingXray(serverId)
    try {
      await serverAPI.syncXray(serverId)
      message.success('xray 配置同步成功')
    } catch {
      message.error('xray 同步失败')
    } finally {
      setSyncingXray(null)
    }
  }
```

- [ ] **Step 5: 在操作列新增同步按钮**

找到操作列 render（约第 455-470 行），在 `ReloadOutlined` 按钮之后、`Popconfirm 删除` 之前插入：

```tsx
      <Button
        size="small"
        icon={<SyncOutlined />}
        title="同步 xray 配置"
        loading={syncingXray === record.id}
        onClick={(e) => handleSyncXray(record.id, e)}
      />
```

最终操作列结构：
```tsx
<Space>
  <Button size="small" type="primary" icon={<CloudUploadOutlined />} title="安装 Agent" onClick={(e) => { e.stopPropagation(); handleInstallClick(record); }} />
  <Button size="small" icon={<EditOutlined />} title="编辑" onClick={(e) => { e.stopPropagation(); handleEditClick(record); }} />
  <Button size="small" icon={<TeamOutlined />} title="账号管理" onClick={(e) => { e.stopPropagation(); handleOpenAccountModal(record); }} />
  <Button size="small" icon={<ReloadOutlined />} title="重启 xray" loading={restartingXray === record.id} onClick={(e) => handleRestartXray(record.id, e)} />
  <Button size="small" icon={<SyncOutlined />} title="同步 xray 配置" loading={syncingXray === record.id} onClick={(e) => handleSyncXray(record.id, e)} />
  <Popconfirm title="确定删除?" onConfirm={() => handleDelete(record.id)}>
    <Button size="small" danger icon={<DeleteOutlined />} title="删除" onClick={(e) => e.stopPropagation()} />
  </Popconfirm>
</Space>
```

- [ ] **Step 6: 编译验证**

```bash
cd /home/jat-id/Project/V2rayDash/frontend
npm run build 2>&1 | tail -5
```

预期：末尾含 `built in`，无 TypeScript 错误。

- [ ] **Step 7: Commit**

```bash
cd /home/jat-id/Project/V2rayDash
git add frontend/src/services/api.ts frontend/src/pages/servers/index.tsx
git commit -m "feat(frontend): add sync-xray icon button to server actions"
```

---

## Task 3: handleAssign 改为串行 + 自动同步

**Files:**
- Modify: `frontend/src/pages/servers/index.tsx:182-202`

- [ ] **Step 1: 替换 handleAssign 函数体**

找到当前 `handleAssign`（第 182-202 行）：

```ts
  const handleAssign = async () => {
    if (!selectedServerForAccounts || assignSelected.length === 0) return
    setAssignSubmitting(true)
    try {
      const results = await Promise.allSettled(
        assignSelected.map(id =>
          subscriptionAPI.addAccount(id, { server_id: selectedServerForAccounts.id, auto_create: true })
        )
      )
      const succeeded = results.filter(r => r.status === 'fulfilled').length
      const failed = results.filter(r => r.status === 'rejected').length
      if (failed === 0) {
        message.success(`成功分配 ${succeeded} 个订阅`)
      } else {
        message.warning(`${succeeded} 个成功，${failed} 个失败`)
      }
      await loadAssignData()
    } finally {
      setAssignSubmitting(false)
    }
  }
```

替换为：

```ts
  const handleAssign = async () => {
    if (!selectedServerForAccounts || assignSelected.length === 0) return
    setAssignSubmitting(true)
    let succeeded = 0, failed = 0
    try {
      for (const id of assignSelected) {
        try {
          await subscriptionAPI.addAccount(id, {
            server_id: selectedServerForAccounts.id,
            auto_create: true,
          })
          succeeded++
        } catch {
          failed++
        }
      }
      if (failed === 0) {
        message.success(`成功分配 ${succeeded} 个订阅`)
      } else {
        message.warning(`${succeeded} 个成功，${failed} 个失败`)
      }
      try {
        await serverAPI.syncXray(selectedServerForAccounts.id)
      } catch {
        message.warning('xray 同步失败，请手动点击同步按钮')
      }
      setAssignSelected([])
      await loadAssignData()
    } finally {
      setAssignSubmitting(false)
    }
  }
```

- [ ] **Step 2: 编译验证**

```bash
cd /home/jat-id/Project/V2rayDash/frontend
npm run build 2>&1 | tail -5
```

预期：末尾含 `built in`，无 TypeScript 错误。

- [ ] **Step 3: Commit**

```bash
cd /home/jat-id/Project/V2rayDash
git add frontend/src/pages/servers/index.tsx
git commit -m "fix(frontend): make batch assign sequential and auto-sync xray after completion"
```

---

## 验收检查

1. **手动同步**：服务器页操作栏出现第 6 个图标（SyncOutlined），点击后有 loading 态，成功显示「xray 配置同步成功」
2. **批量分配串行**：关联订阅 Tab 勾选多个订阅后点「确认分配」，请求逐个发送（Network 面板可见），结束后自动触发一次 sync-xray
3. **后端同步端点**：`curl -X POST http://localhost:8080/api/servers/<id>/sync-xray -H "Authorization: Bearer <token>"` 返回 `{"message":"同步成功"}`
